<h2>THIS IS THE LIST OF USERS</h2>
<ul>
<%= button_to "New User", new_user_registration_path %>
<% @users.each do |user| %>
    <li>
   
    <img src="<%= user.photo %>">
    <b><%= link_to user.name, user_path(user)%></b>
    Number of Posts: <%= user.posts_counter%>
    </li>
    <% end %>
</ul>
